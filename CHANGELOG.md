## Unreleased
- 新增群级世界状态自动推送，覆盖指定钢铁全能裂缝、Lotus 礼物、特殊活动、官方新闻、突击、执刑官和奸商库存
- 新增 `/推送开启`、`/推送关闭`、`/推送状态`，并持久化每个群的去重状态
- 新增深层科研、时光科研查询，兼容官方 `Conquests` 与 WarframeStat 聚合结构
- 修复 WarframeStat 科研类型码异常空格及钢铁奖励小写字段、专用端点降级解析
- 按灰机 Wiki 译名汉化深层科研、时光科研的任务偏差与风险变量
- 修复短内容卡片达到最小截图高度时底图未铺满、下方出现白色区域的问题
- 修复 AstrBot 将普通群消息标记为唤醒流程时，无前缀指令只在私聊生效的问题
- 修复双衍王境轮回奖励对 `EndlessXpSchedule.CategoryChoices` 新字段的解析
- 裂缝默认按古纪、前纪、中纪、后纪、安魂、全能排序，并可在配置中恢复按剩余时间排序
- 新增可选的全局渲染底图，以及裂缝、价格、世界状态分类覆盖
- 支持本地路径和 HTTP(S) 图片 URL，并在启动阶段完成校验与缓存
- 新增可配置遮罩、面板透明度、背景适配方式、文字模式和毛玻璃开关/模糊程度
- 统一资料查询为 `/wk <关键词>`，通过本地别名映射查询灰机 Wiki；移除旧静态资料、掉落和遗物命令
- 重构本地映射命令为管理员使用 `/wfmap`、`/wfmapdel`，公开使用 `/wfmapq`；映射不再依赖 `warframe.market` 校验，可覆盖非交易词条
- 移除 `wk` 的紫卡别名含义，紫卡查询统一使用 `/wmr` 与 `/wr`
- 灰机 Wiki 查询增加精确命中、未找到、服务不可用三态处理；后两种情况均降级返回搜索页并区分提示

## v0.3.2
- 优化无前缀指令触发
- wm指令添加等级参数
- wmr指令添加循环数参数(零洗)
- 修复部分商品解析错误
